# Local Claude Code permission grants (machine-specific, not shared).
.claude/settings.local.json

# Stray git worktrees (the loop uses branch-in-main, but never commit a worktree dir).
.worktrees/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
*.egg-info/
.env
