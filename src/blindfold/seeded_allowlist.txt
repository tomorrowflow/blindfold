# Seeded allowlist (ADR-0023, issue #71). Evidence-first: the exact tokens a real
# one-shot `claude -p` session minted through the proxy (gemma4:e4b) on 2026-07-09,
# filtered by the ADR-0023 curation rule -- public vendor/framework/tool identifiers,
# implausible as a protected referent when unregistered. One entry per line; blank
# lines and #-comments ignored. An entry may itself be a multi-word phrase (ADR-0023
# issue #294 update), matched span-wise rather than as a single token. Semantics
# identical to a learned reject (ADR-0010): suppresses novelty discovery only, never
# protection (a registered Term equal to a seeded token is still blindfolded).
#
# AI vendors / models
Claude
Anthropic
Sonnet
Opus
Haiku
Fable
Gemini
Llama
Mistral
Cohere
Ollama
#
# Languages / frameworks / dev infrastructure
React
Git
Github
Slack
Linear
Grafana
Prisma
Express
Tailwind
Django
Recharts
Electron
Mac
Windows
Next
Vue
Playwright
Supabase
Postgres
FastAPI
OpenBao
Spring
Boot
#
# Common agent tool names in prose
Grep
Glob
#
# GLiNER cascade ORG-precision audit (ADR-0033 §2 update, issue #281). Evidence:
# tests/fixtures/gliner_org_probe_corpus.json -- a probe corpus run through the
# real GlinerOnnxClassifier.classify_span. Each is a confirmed organization/person
# false positive AND a purpose-coined public brand identifier with no ordinary-
# English or personal-name collision (see the corpus's per-entry rationale for the
# excluded counter-examples that failed this bar -- Kafka/Jenkins, both a common
# human surname GLiNER's own "person" label corroborates: re-judged against
# ADR-0023's actual curation rule by issue #356 and still excluded, since a common
# surname is exactly the plausible-unregistered-referent case that rule protects,
# not merely a failure of this audit's own narrower non-collision bar).
# "Vault" was excluded here too under this audit's stricter "no ordinary-English-
# word/no internal-codename plausibility" bar -- issue #356 supersedes that call
# the same way "Transit" superseded its own 2026-07-10 rejection below: measured
# against ADR-0023's actual rule (public identifier, implausible as a protected
# referent when unregistered) rather than the audit's own additional bar, in this
# project's own context (OpenBao's key-custody prose names it as the project
# OpenBao forked from) it is exactly the React/Grafana/Prisma class. See "Vault"
# in the #74 run-12 batch below.
# "Transit" is the issue's own cited live finding (OpenBao's key-wrapping secrets
# engine) and supersedes the 2026-07-10 #87 rejection, which considered it only as
# bare generic prose, not this project's own named component.
Transit
Docker
Nginx
Datadog
Figma
Netlify
Heroku
Cloudflare
Bitbucket
Firefox
Kubernetes
#
# #74 run 12 review-inbox batch (issue #356). Two of run 12's four false
# positives are public product names measured against ADR-0023's actual
# curation rule (public framework/vendor/tool identifier, implausible as a
# protected referent when unregistered) rather than the stricter GLiNER-audit
# bar above: "Vault" (HashiCorp's -- named in this project's own key-custody
# prose as what OpenBao forked from) and "Presidio" (Microsoft's -- arriving
# via the driving harness's own auto-loaded memory index). Seeding both:
# 10 mints/60% precision -> 8 mints/75%; "Vault"'s mint also produced one of
# run 12's three blocked exchanges, so this is an availability fix too.
Vault
Presidio
#
# MCP / collaboration-tool vendors (issue #297). Evidence: the #74 live-verify
# run 6 egress audit -- these public product names, named in the harness's own
# safety-rules text, minted provisional surrogates in some hops (e.g. Jira,
# Asana) while the same names egressed in plaintext in other hops. Slack,
# Linear, Figma and Datadog were already seeded by earlier issues (#137/#87);
# this batch adds the rest of run 6's named set -- except "Notion" (run 6's
# own list names it too), which the issue #281 GLiNER audit
# (tests/fixtures/gliner_org_probe_corpus.json) already measured and
# explicitly excluded under this same ADR-0023 curation rule: an ordinary
# English dictionary word ("an idea/notion") with no single dominant
# public-brand reading, plausible as a generic project/codename -- the same
# collision class as Confluence/Zoom/Stripe, left out there for the
# identical reason. Seeding it here would silently re-litigate that decision
# without new evidence; flagged in this issue's handoff notes for a human call
# instead of resolved unilaterally.
Asana
Jira
Canva
Intercom
HubSpot
Atlassian
PagerDuty
Box
#
# Blindfold's own identifiers (project + tooling), public once shipped
Blindfold
Sandcastle
Supacode
#
# Common English/German words capitalized in agentic prose (ADR-0032 dismissal-log
# curation). Evidence: BLINDFOLD_L3_DISMISSAL_LOG from live Claude Code traffic
# through the GLiNER cascade on 2026-07-20 -- generic nouns/verbs the inner LLM
# dismissed but GLiNER intermittently auto-confirmed into the review inbox. Each is
# a common dictionary word, implausible as a protected referent when unregistered.
Code
Refactor
Index
Design
Artifacts
#
# Full dismissal-log curation pass (ADR-0032). Evidence: BLINDFOLD_L3_DISMISSAL_LOG
# from live Claude Code / agentic traffic through the GLiNER cascade, 2026-07-20 --
# capitalized common words the inner LLM dismissed as non-entities, deduplicated.
# "Projekt"/"Polarstern" excluded by human review (possible real codename).
Activates
Add
Agents
All
Asserts
Assist
Assistant
Audits
Bad
Bootstrap
Break
Build
Button
Canonical
Catch
Caveat
Compact
Complete
Contents
Control
Create
Cuts
Debug
Deep
Define
Directory
Disciplined
Dockerfile
Documents
Drive
Dual
English
Explore
Explores
Find
General
Generate
Generic
Good
Grilling
Helps
High
Hooks
Ignore
Implement
Independent
Initialize
Instructions
Interview
Investigate
Iterate
Korean
Launch
Launches
Load
Login
Managed
Management
Medium
Merge
Messages
Mobile
Note
Omit
Prefer
Primary
Processing
Produces
Recalled
Reference
Refuse
Report
Reproduce
Return
Returns
Reveal
Review
Run
Runner
Runs
Runtime
Scan
See
Seed
Sending
Serves
Setup
Software
Spawned
Specify
Steps
Storing
Tag
Teaches
Test
Title
Today
Tool
Treat
Triage
Trigger
Turn
Turns
Ultra
Upgrades
Use
Users
Verify
Version
Write
#
# Calendar months (English) -- routinely capitalized, never a protected referent
# unless explicitly registered as a Term (which still wins, ADR-0023). May/June/
# August double as given names; the registration safety net covers that case.
January
February
March
April
May
June
July
August
September
October
November
December
#
# Ordinal numbers spelled out -- capitalized sentence-initially or in headings/lists.
First
Second
Third
Fourth
Fifth
Sixth
Seventh
Eighth
Ninth
Tenth
#
# Cardinal numbers spelled out. NB: bare digits (1..10) are intentionally NOT listed
# -- the L3 candidate selector only flags tokens matching [A-Z][a-z]+ (l3.py
# _CAPITALIZED_RE), so a digit is never a candidate and needs no allowlisting.
One
Two
Three
Four
Five
Six
Seven
Eight
Nine
Ten
#
# Blindfold's own glossary vocabulary (CONTEXT.md's Glossary, ADR-0023's curation
# rule applied fresh -- issue #353). "Surrogate" is a measured false positive in
# #74 runs 10 and 11: a Blindfold-development session reads this project's own
# docs as tool results, and every one of these terms appears Title-Case
# throughout them. Curation rule unchanged: implausible as a protected referent
# when unregistered -- a registered Term equal to one of these still blindfolds
# (L2 wins before the allowlist is consulted). Multi-word entries are matched
# span-wise (ADR-0023, issue #294): a phrase like "Store directory" (whose
# second word is lowercase and never itself a candidate token) suppresses its
# own leading token "Store" only inside that literal phrase, not standalone --
# "Blindfold"/"Merge"/"Bootstrap"/"Setup"/"Transit" are already seeded above for
# unrelated reasons and are also glossary headings; not repeated here.
# Three glossary headings are deliberately NOT seeded, with reasons recorded in
# blindfold.allowlist_seed.GLOSSARY_EXCLUSIONS and pinned by
# test_glossary_allowlist_sync.py: "Re-identify" and "Fail-closed" (hyphenated,
# no internal whitespace -- mechanically can't become a working allowlist entry),
# and "Egress" (collides with a real public security/privacy vendor name,
# unlike this project's own vocabulary).
Restore
Entity
Term
Org unit
Surrogate
Mapping
Entity graph
Data directory
Store directory
Variation
Retired surrogate
Relationship
Coherent surrogate world
Detection layers
Candidate span
Hop
Workspace
Role
Supervisor
Launch environment
Seed bundle
Sample data
Novel entity
Dismissal
Dismissal log
Review inbox
Provisional surrogate
Learning loop
Allowlist
Declared tool vocabulary
Suppression
Payload-region confinement
Case-inconsistency suppression
Positional case heuristic
Closed-world restore
Surrogate component
Verify pass
Declared collision
Sliding-window restore
Mapping cipher
Store key
Blind index
Unprotected mode
Audit event
Scrubbed reason
Processing trace
Supervisor log
Diagnostic session
Exchange capture
