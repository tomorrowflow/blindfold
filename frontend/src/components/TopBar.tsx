// Placeholder top bar (issue #93): white chrome, no content yet — the workspace
// switcher and role chips land with the shell-chrome slice (issue #95).
export function TopBar() {
  return (
    <header className="bf-topbar">
      <span>Blindfold Management</span>
    </header>
  );
}
