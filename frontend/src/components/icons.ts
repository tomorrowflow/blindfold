// Vendored icon set (ADR-0026): a Lucide subset bundled at build time, replacing
// the prototype's proprietary icons and any icon-font CDN. Single re-export point
// so the sidebar's icon choices stay in one place.
export {
  ChevronsLeft,
  ChevronsRight,
  House,
  Users,
  Share2,
  Inbox,
  ScrollText,
  ShieldCheck,
  Settings,
  // Shell chrome (issue #95)
  ChevronDown,
  Check,
  Lock,
  X,
  // Shell chrome fidelity (issue #114)
  Layers,
  EyeOff,
  // Home/Status view (issue #96)
  CheckCircle2,
  AlertTriangle,
  Cloud,
  Bot,
  KeyRound,
  Database,
  ArrowRight,
  // Audit log view (issue #102)
  Calendar,
  UserRound,
  // Access view (issue #103)
  Plus,
  // Settings -> Detection view (issue #147)
  RefreshCw,
  // Processing trace view (ADR-0035, issue #151)
  Activity,
  CloudOff,
  // Entity list actions polish (issue #175)
  Pencil,
  Merge,
} from "lucide-react";
